# 커리큘럼 수강료 표시 및 결제 버튼 개편

## 작업 일시
2026-04-12

## 관련 파일
- `src/app/curriculum/page.tsx`

## 주요 변경 사항
1. **수강료 제거 및 [자세히 보기] 버튼 추가**
   - 기존의 헤더 우측 상단에 표시되던 `월별 수강료 금액` 텍스트와 값을 제거하였습니다.
   - 해당 자리에 `next/link` 요소의 `[자세히 보기]` 버튼을 추가하여 각 부서별 상세 페이지(초등부, 중등부, 고등부)로 이동하도록 변경하였습니다.

2. **하단 결제 버튼 변경**
   - 하단에 위치했던 `{부서명}수업 온라인 결제` 버튼을 제거하였습니다.
   - 기존의 `<button>` 태그를 `next/link` 객체로 변경하고, `[자세히보기]` 버튼으로 교체하여 각 부서의 상세 커리큘럼 페이지로 연결되도록 구현하였습니다.
   - 미사용 함수 `tabNameToString`을 제거하였습니다.

## 각 탭별 연결 경로
- 고등부: `/curriculum/high-school`
- 중등부: `/curriculum/middle-school`
- 초등부: `/curriculum/elementary-school`
